[gd_resource type="ShaderMaterial" load_steps=2 format=2]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform vec2 target = vec2(0.5f);
uniform float intensity: hint_range(0f, 1f);

void fragment(){
	vec4 t = texture(SCREEN_TEXTURE, SCREEN_UV);
	if(UV.x <= target.x + SCREEN_PIXEL_SIZE.x && UV.x >= target.x - SCREEN_PIXEL_SIZE.x){
		COLOR = vec4(1f, 0f, 0f, intensity);
	}else if(UV.y <= target.y + SCREEN_PIXEL_SIZE.y && UV.y >= target.y - SCREEN_PIXEL_SIZE.y){
		COLOR = vec4(1f, 0f, 0f, intensity);
	}else{
		COLOR = t;
	}
}"

[resource]
shader = SubResource( 1 )
shader_param/target = Vector2( 0.5, 0.5 )
shader_param/intensity = 0.0
